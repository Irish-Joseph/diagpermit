# Synthetic secret test corpus.
#
# Every value in this directory is FAKE and generated for testing.
# Never commit real production credentials here.
#
# Files:
#   jwt.log             JWT-like tokens
#   github.log          GitHub-style tokens
#   aws.log             AWS-style credentials
#   db.log              Database passwords and connection strings
#   auth.log            Authorization headers
#   pem.log             PEM private key (synthetic, not a real key)
#   connections.log     Mixed connection strings
#   emails.log          Email addresses
#   ipv4.log            IPv4 addresses
#   ipv6.log            IPv6 addresses
#   home.log            Home-directory usernames
#   mac.log             MAC addresses
#   benign.log          Content that must NOT be altered
